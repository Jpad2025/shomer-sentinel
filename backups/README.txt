# Carpeta backups (legacy)

- Salida de backup_system.py (tar.gz de BD y backend). Se mantiene aquí por compatibilidad.
- Los backups oficiales con Restic están en: /srv/shomer_backups/staging (partición p7).
- Para replicar: puede configurarse backup_system.py para escribir en /srv si se desea.
